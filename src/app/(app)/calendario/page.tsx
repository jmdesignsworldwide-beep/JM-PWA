import { ModulePlaceholder } from "@/components/layout/module-placeholder";

export const metadata = { title: "Calendario" };

export default function CalendarioPage() {
  return (
    <ModulePlaceholder
      title="Calendario"
      subtitle="Calendario general multi-tipo y multi-marca."
      phase="Fase 5"
    />
  );
}
